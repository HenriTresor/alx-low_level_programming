#include<stdio.h>
int main(void)
{
/**
 * main - Entry point
 *
 * This function is the entry point of the program. It prints a message to
 * the standard output using the `puts` function and returns 0 to indicate
 * success.
 *
 * Return: Always 0 (Success)
*/
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
